import { PageTransition } from '@/components/PageTransition';
import Landing from './Landing';

const Index = () => {
  return (
    <PageTransition>
      <Landing />
    </PageTransition>
  );
};

export default Index;
